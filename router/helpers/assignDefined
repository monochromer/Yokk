'use strict'

module.exports = (checkedValue, assignedValue) => {
    if (typeof assignedValue === 'undefined') {
        assignedValue = checkedValue;
    };
    if (typeof checkedValue === 'undefined') {
        return 'not assigned';
    };
    return checkedValue;
}
