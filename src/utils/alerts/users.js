export function addUserSuccess(login) {
  return 'Success! User "' + login + '" added.';
}

export function addPhotoSuccess(login) {
  return "Photo of " + login + " has been succesfully uploaded.";
}

export function updateUserSuccess(login) {
  return "User " + login + " has been succesfully changed.";
}
